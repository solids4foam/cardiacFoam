#!/bin/bash
cd ${0%/*} || exit 1    # run from this directory

# Stop at first error so a failed solver is not silently skipped
set -e

wmake all solvers
wmake all utilities
#(cd scripts && ./Allwmake)

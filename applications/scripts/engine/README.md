# Shared Engine

Upper-layer shared runtime package for orchestration and step execution.

Package:

- `src/cardiac_engine/`

Primary API:

- `CardiacPreprocEngine`

The core domain/step implementations remain in:

- `../cardiac_preproc/src/cardiac_preproc/`

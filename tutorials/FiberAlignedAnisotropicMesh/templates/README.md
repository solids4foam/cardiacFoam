# Templates

Contains the base OpenFOAM case directories used as templates.
The setup scripts copy these into the `simulations/` folder before injecting the specific meshes and fiber fields.

- `monodomainHeartTissue`: Base `cardiacFoam` template with `electroProperties`, solver settings, and basic dictionaries.

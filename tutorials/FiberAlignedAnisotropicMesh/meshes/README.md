# Meshes and Geometries

Contains all intermediate and final mesh files for the pipeline:

- `gmsh/`: Base geometry and tetrahedral meshes generated via Gmsh.
- `metrics/`: Background metric field `.pos` files defining anisotropic element sizing based on fiber alignment.
- `vtu/`: Inputs and outputs for the `cobiveco` MATLAB workflow.

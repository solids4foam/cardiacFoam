# gradient_reconstruction (eikonalECG)

This study directory tracks configurations and results for the gradient_reconstruction study.

All generated outputs and metric archives are saved to `results/` (which is git-ignored).

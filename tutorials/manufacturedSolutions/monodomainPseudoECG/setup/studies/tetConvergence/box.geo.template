// Unit-cube [0,1]^3 tetrahedral mesh for the monodomain manufactured MMS.
//
// Frontal-Delaunay 3D generation (anatomically-oriented quality family),
// with Netgen optimisation and extra smoothing passes. The characteristic
// length placeholder __LC__ is substituted by render_tet_geo (lc = 1/N).

SetFactory("OpenCASCADE");

lc = __LC__;

Box(1) = {0, 0, 0, 1, 1, 1};

Physical Volume("internal") = {1};
Physical Surface("boundary") = {1, 2, 3, 4, 5, 6};

Mesh.MeshSizeMin = lc;
Mesh.MeshSizeMax = lc;
Mesh.Algorithm3D = 4;      // Frontal: anatomically-oriented quality family
Mesh.Optimize = 1;
Mesh.MshFileVersion = 2.2; // gmshToFoam requires the legacy msh2 format
Mesh.OptimizeNetgen = 1;
Mesh.Smoothing = 100;
